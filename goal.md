Story: #0

## Objective

Create a new file called `pipeline-test.txt` in the project root.

## Required Change

Create the file `pipeline-test.txt` with this exact content:

```
pipeline test
```

## Acceptance Criteria

- `pipeline-test.txt` exists in the project root with the content `pipeline test`.
- No other changes to any files.