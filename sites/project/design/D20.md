# Decision 20 — MCP surface: drop publish/unpublish, thread `created_by`, flip visibility, serve the live folder

**Decision.** The `ikigenba_sites_*` tool table drops the publish lifecycle and
reflects the new model: a site is created (private by default), its files are
edited *live* in its served directory, its visibility is flipped with one tool,
and it is deleted.

**Tools removed:** `publish`, `unpublish`.

**Tools added:** `set_visibility` — flip a site public↔private.

```
set_visibility(name, public: bool)
  → Store.SetVisibility(name, public) + Layout.Move(name, public), in lockstep.
    Returns the site's JSON projection with its new "url".
```

**Tools changed:**

- `create(name)` — now records the creator from the request `Identity` (the
  nginx-injected owner email) into `created_by`, and creates the site's directory
  at `SiteDir(false, name)` (private by default). Signature of the store call
  becomes `store.Create(ctx, name, identity.Owner)`.
- `delete(name)` — removes the row **and** the site's on-disk directory
  `SiteDir(site.Public, name)` (no unpublish step; no working tree). Idempotent.
- `list` / `describe` — describe sites in public/private + creator terms; the
  lifecycle text drops "working tree" and "publish/unpublish". `describe`'s
  `serves_at` reflects `<baseURL>public/<name>/` or `<baseURL>private/<name>/`.
- The **file tools** (`file_write`, `file_read`, `file_edit`, `file_glob`,
  `file_grep`, `file_list`, `mkdir`) and **`sync`** operate on the site's **live
  served directory** — `SiteDir(site.Public, name)` resolved from the DB row — not
  a working tree. A write is immediately visible under the served tree. The tools
  first `Get` the site to learn its visibility, then confine under that directory
  via `internal/files.ConfinePath` (unchanged confinement, new root).

**`renderSite`** projects the new fields — `name`, `public`, `created_by`,
`url`, `created_at`, `updated_at` — where `url` is `<baseURL>public/<name>/` when
public else `<baseURL>private/<name>/`. `tier`/`published`/`published_at` drop out
of the projection. `errResult` drops the `ErrInvalidTier` case.

**Rejected.**
- **Keeping `publish`/`unpublish` as no-ops or aliases of `set_visibility`.** The
  operator removed the lifecycle; leaving dead verbs invites the old mental model.
  A site that exists is served; visibility is the only control.
- **A separate working directory the file tools edit.** Removed with D16 — edits
  are live; the tools address the served directory directly.
- **Taking `created_by` as a tool argument.** The creator is the authenticated
  caller; reading it from the request `Identity` prevents spoofing and needs no
  argument.
- **A `public` argument on `create`.** New sites default **private** (safe: never
  auto-expose a just-created, empty site to the world); the owner flips to public
  with `set_visibility` once it's ready.

**Verification.**
- R-RDBZ-AE4J — the tool table returned by `Tools(...)` contains **no** tool named
  `publish` and **no** tool named `unpublish`.
- R-RFRS-1XLX — calling `create` with a request `Identity` whose owner is
  `alice@example.com` persists `created_by == "alice@example.com"`, surfaced on the
  returned site and on a subsequent `list`.
- R-RGZO-FPCM — calling `set_visibility(name, true)` on a private site flips the
  stored `public` flag to true, relocates its directory to `SiteDir(true, name)`,
  and returns a site whose `url` ends `public/<name>/`; `set_visibility(name, false)`
  reverses all three.
- R-RI7K-TH3B — a `file_write` to a site resolves under `SiteDir(site.Public, name)`
  (the live served directory): after writing `index.html` to a public site, the
  file exists at `SiteDir(true, name)/index.html` and is served by D17's handler —
  there is no separate working-tree copy.
- R-RJFH-78U0 — `delete(name)` removes both the row (a subsequent `get` yields
  not-found) and the on-disk directory `SiteDir(site.Public, name)` (it no longer
  exists).
