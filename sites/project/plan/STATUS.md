# sites — Plan Status

This is the **manifest**: one line per phase in build order, and the **only**
place a phase's status marker lives. Each phase line begins with the literal word
`Phase` and carries `✅` (done) or `⬜` (not started). The build loop finds its
next unit of work with `grep -nE '^Phase .* ⬜' project/plan/STATUS.md | head -1`,
reads only that phase's `project/plan/phase-NN.md`, builds it, and on completion
flips that one marker. This file deliberately carries **no bare status glyph**
anywhere but on a phase line, so the anchored grep matches only phase lines.

Phase 01 ✅ realizes D1, D2, D3 — landing handler + embedded Carbon template/assets in `internal/web`, wired at `GET /{$}` (ungated in-process) in `cmd/sites/main.go`
Phase 02 ✅ realizes D4 — nginx fragment: add the exact-match session-gated `= /srv/sites/` landing root beside the existing static tiers, validated by a content-assertion test
Phase 03 ✅ realizes D5 — state the standardized landing card in `cmd/sites/main.go`'s package doc comment (structural; docs-only — sites has no "no UI" claim to purge)
Phase 04 ✅ realizes D6 — conform the landing page (markup + tokens) to the cron canonical template; per-service data only (eyebrow + description + title); structural content check
Phase 05 ✅ realizes D7 — add the top-left Home link to the dashboard landing page; covers R-HOME-9S3W
Phase 06 ✅ realizes D8 — self-serve the landing fonts and eliminate the FOUT: relativize the `tokens.css` link, `font-display: swap`→`optional` + self-served `src` in tokens.css, preload display+body fonts with `crossorigin`, and add the session-gated nginx `location /srv/sites/static/` (proxied to upstream `/static/`); MUST run after cron's FOUT phase (byte-pinned to cron canonical); covers R-629P-84O5, R-63HL-LWEU, R-64PH-ZO5J, R-65XE-DFW8, R-675A-R7MX
Phase 07 ✅ realizes D9 — adopt the shared `registry`: add `require registry` + committed `replace registry => ../registry` to `go.mod`, and in `cmd/sites/main.go` swap the own-port literal `Port: 3004`→`registry.MustPort("sites")` and the dropbox default `"http://127.0.0.1:3200"`→`registry.BaseURL("dropbox")` (resolved by name at the composition root; behavior-preserving; nginx fragment literal deliberately excluded); covers R-7K2P-QN4D, R-7L9F-XW3H, R-7M4C-BV8J, R-7N6R-TZ2Q
Phase 08 ✅ realizes D10 — build the native `internal/files` package: confined filesystem ops as plain Go over Go types (symlink-resolving `ConfinePath` + `ErrEscapes` sentinel, `Read`/`Write`/`Edit`/`Glob`/`Grep`/`List`/`Mkdir`), ported from `prompts`' worker bodies, no agentkit/JSON/agent framing, standalone unit-tested; covers R-027Y-BQ1I, R-03FU-PHS7, R-04NR-39IW, R-05VN-H19L, R-073J-UT0A, R-08BG-8KQZ, R-09JC-MCHO, R-0AR9-048D, R-0D71-RNPR, R-0EEY-5FGG
Phase 09 ✅ realizes D11 — rewire the MCP file tools onto `internal/files` and drop `agentkit`: delete the `agentkit/wire`+`tools` bridge in `internal/mcp/files.go`, hand-write the four file-tool schemas, cleaner structured results, unify confinement on `files.ConfinePath` (sentinel→`path_escapes_working_dir`), point `sync` at `files.Write`, and remove `require`/`replace agentkit` from `go.mod` (surface-preserving; orphaned repo-root `agentkit/` left on disk); covers R-0FMU-J775, R-0GUQ-WYXU, R-0I2N-AQOJ, R-0JAJ-OIF8, R-0KIG-2A5X
Phase 10 ✅ realizes D10 — fix `internal/files.Glob` recursive `**` matching: walk the confined search base and match each base-relative path one `/`-separated segment at a time (`*`/`?`/`[…]` never cross `/`; `**` spans any run of segments including zero) instead of delegating to `filepath.Glob`, so `**/*.css` finds `.css` at the base and any depth while single-segment `*` stays non-recursive; base-relative/sorted/`[]string{}`/`ErrEscapes` guarantees and the three existing Glob tests preserved; covers R-3ZP8-T0GP, R-40X5-6S7E
Phase 11 ✅ realizes D12 — serve the web surface from `share/www` through the chassis (de-embed via `Spec.WWW`): move `landing.html`+`static/` to `sites/share/www` (same bytes), set `WWW:true`, render `GET /{$}` via `rt.WWW().Render`, delete `internal/web` and the service-side `GET /static/` mount (chassis auto-mounts it), relocate the landing/asset/nginx tests to `cmd/sites` loading the real `share/www` via `appkit/web`; retained D1/D2/D3/D6/D7/D8 + D4 nginx ids ride the new substrate; the `bin/start` `SITES_WWW_PATH` export is the one boundary-crossing line, verified by the live smoke not the Go suite; covers R-0SF5-VPQF, R-0TN2-9HH4
Phase 12 ✅ realizes D13 — MCP surface over `appkit/mcp`: reshape `internal/mcp` into `Instructions`+`Tools(store,layout,baseURL,mirror) []mcp.Tool` (the fourteen domain tools, wire-unchanged) + `NewHandler(...,rt) (http.Handler,error)`, delete the local JSON-RPC transport / local `health` / `SetMirrorClient` seam (mirror client becomes a constructor param), chassis supplies `health`+`reflection` so sites gains a `reflection` tool (empty graph — neither producer nor consumer); D11's R-0KIG-2A5X updated in place to the 16-tool partition; existing domain-tool behavioral tests swap only the handler constructor; covers R-0UUY-N97T, R-P21E-0285
Phase 13 ✅ realizes D14 — delete the `internal/db` `Open`/`Migrate` shim (keep only the embedded `FS` + `migrations_load_test.go`; the three domain test harnesses — `internal/mcp/tools_test.go`, `internal/sites/store_test.go`, `internal/sites/publish_test.go` — call `appkit/db` directly, no assertion changes), normalize the composition root (move the `Handlers` closure inside `sitesSpec()` so it returns a fully-formed `Spec` and `main()` is just `appkit.Main(sitesSpec())` — no post-construction `.Handlers` mutation; keep the `sitesSpec()` builder for the main_test.go callers; byte-identical relocation), and true up `sites/AGENTS.md` if present (structural; composition-root/docs/shim — no ids)
Phase 14 ✅ realizes D17, D16 — in-process static server `internal/serve` (index-map, no-listing, confined, trailing-slash redirect) + `Layout.SiteDir`/`SiteBase`, wired ungated at `GET /public/` and `GET /private/`, additive (nothing old removed); covers R-QZX3-2WYW, R-R14Z-GOPL, R-R2CV-UGGA, R-R3KS-886Z, R-R4SO-LZXO, R-R60K-ZROD, R-QV1H-JU04
Phase 15 ✅ realizes D15, D16 — additive migration adding `public`+`created_by`, `Site` gains the fields, `Create(name, createdBy)` defaults private, `SetVisibility` flips the flag, `Layout.Move` relocates the dir (old lifecycle columns/methods retained for green); covers R-QRDS-EIS1, R-QSLO-SAIQ, R-QTTL-629F, R-QW9D-XLQT
Phase 16 ✅ realizes D20 — MCP surface switch: drop `publish`/`unpublish` tools, add `set_visibility`, thread `created_by` from Identity, retarget file tools/`sync`/`delete` onto `SiteDir(site.Public,name)`, `renderSite` projects `public`/`created_by`/`url`; covers R-RDBZ-AE4J, R-RFRS-1XLX, R-RGZO-FPCM, R-RI7K-TH3B, R-RJFH-78U0
Phase 17 ✅ realizes D18 — nginx fragment `sites/etc/nginx.conf`: public/private tiers `proxy_pass` to the process (was `alias`), private keeps `auth_request /_session-authn`, no on-disk state reference; content-assertion test; covers R-R78H-DJF2, R-R8GD-RB5R, R-R9OA-52WG
Phase 18 ✅ realizes D19 — landing `GET /{$}` lists sites from `store.List` (slug/public-private/creator/created-at) via `landing.html`, empty-state safe; covers R-RAW6-IUN5, R-RC42-WMDU
Phase 19 ✅ realizes D15, D16 — cleanup: drop `tier`/`published`/`published_at` (new migration), delete `Publish`/`Unpublish`/`ErrInvalidTier`/`publish.go` and the working/served/symlink layout helpers, `Site`/`scanSite` to the final column set; covers R-QQ5W-0R1C, R-QYP6-P587
Phase 20 ✅ realizes D6, D19 — landing layout rewrite + slug links: collapse name+version to one 28px heading line, drop the Service/Version/API card (`POST /mcp`) and the `Sites` h2, reword the lead, and render each slug as an anchor to the site's absolute front-door URL (shared `baseURL`, == the MCP `url`); covers R-WKGI-FVFJ, R-WLOE-TN68, R-WMWB-7EWX
Phase 21 ✅ realizes D20 — `create(name, public?)` births a site at the chosen visibility in one insert (default private; `Store.Create` gains `public`), and `sync` requires an already-created site (removes create-or-reuse and the empty-`created_by` insert; `not_found` otherwise); covers R-554R-3MBC, R-56CN-HE21
Phase 22 ✅ realizes D21 — adopt the MCP self-discovery convention: rewrite the Tier-0 `instructions` with routing vocabulary + a guide pointer, replace `describe` with a read-only embedded `guide` tool (worked examples), keyword-forward the `create`/`sync` descriptions, and reference the guide in exactly two channels; covers R-57KJ-V5SQ, R-58SG-8XJF, R-5A0C-MPA4, R-5B89-0H0T, R-5CG5-E8RI
Phase 23 ✅ realizes D22 — `share/www/static/landing.js` pure logic (subsequence-fuzzy `filterSites`, `sortRows` name/createdAt-via-sortable-ts/createdBy + slug tie-break + `nextSort` toggle, page-size-10 `paginate`, `defaultState`/`reduce`/`computeView`) exposed on `globalThis.SitesLanding`, DOM bootstrap behind a `document` guard; add the test-only `github.com/dop251/goja` dep and a goja test that drives the real shipped file; no html/handler/db/nginx change; covers R-HU67-LJBW, R-HVE3-ZB2L, R-HWM0-D2TA, R-HXTW-QUJZ, R-HZ1T-4MAO, R-I1HL-W5S2, R-I2PI-9XIR, R-I3XE-NP9G, R-I55B-1H05, R-I6D7-F8QU, R-I7L3-T0HJ, R-I8T0-6S88, R-IA0W-KJYX, R-IB8S-YBPM, R-7V8B-GA0T
Phase 24 ✅ realizes D19, D6, D22 — wire the control layer into the landing page: add `siteRow.CreatedAtSort`, emit the `#sites-data` JSON island (url == anchor href), render the hidden-until-JS search/Clear/pager controls + `data-sort-key` header hooks, add `initController` (DOM half) and the `<script src="static/landing.js">` reference; structural/render tests over the repo-real `share/www` + `GET /{$}` handler; covers R-IDOL-PV70, R-IEWI-3MXP, R-IG4E-HEOE, R-IHCA-V6F3, R-ICGP-C3GB
