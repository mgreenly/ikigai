# Decision 40 — Conform the landing page to the cron canonical template

**Decision.** wiki's landing page is brought to **visual and structural parity
with the cron landing page**, which is the suite's canonical reference — the one
page that holds up the Carbon style guidelines. cron's page is a centered `main`
with an eyebrow label, a display `<h1>` showing the service name, a one-line lead
paragraph, and a bordered three-cell **Service / Version / API** definition list,
over its own embedded `tokens.css`. The landing page shipped under D39 has its own
markup and token values; this Decision **replaces that markup and those tokens
wholesale with cron's**, keeping only the data that must reflect **this** service.

**Canonical source of truth.** Two files, as committed on `main`:

- `cron/internal/web/landing.html` — the exact page markup + inline `<style>`.
- `cron/internal/web/static/tokens.css` — the Carbon design tokens.

The conform target is **defined by those two files**. There is no frozen second
copy to keep in sync: if cron's canonical page ever changes, this page re-derives
from it by the same diff.

**What changes (markup + tokens only — no Go logic, no handler signature, no
nginx):**

- **`wiki/internal/web/landing.tmpl`** becomes **identical to
  `cron/internal/web/landing.html` except exactly three substitutions.** Every
  other line — the inline `<style>`, the `<h1>{{.Service}}</h1>`, the
  `Service / Version / API` `<dl>` whose API value is `POST /mcp`, and the
  `/static/tokens.css` link — is cron's, byte-for-byte:

  | line | cron (canonical) | wiki |
  |---|---|---|
  | `<title>` | `{{.Service}} · cron` | `{{.Service}} · wiki` |
  | `.eyebrow` text | `Scheduled event emitter` | `Knowledge base` |
  | lead `<p>` | `Cron keeps named schedules in SQLite and emits typed event-plane messages at minute boundaries.` | `Wiki ingests, searches, and answers over a durable knowledge base using retrieval-augmented generation.` |

  The template's data fields are unchanged: `LandingHandler(service, version
  string)` renders `landingData{Service, Version}`, so `{{.Service}}` /
  `{{.Version}}` carry over verbatim from cron's markup.

- **`wiki/internal/web/static/tokens.css`** becomes **byte-identical to
  `cron/internal/web/static/tokens.css`**. The embedded `static/fonts/` woff2 set
  is already the shared set and is unchanged.

- The handler `web.LandingHandler(service, version string) http.HandlerFunc`, the
  `GET /{$}` + `GET /static/` route wiring in `cmd/wiki/main.go`, and the nginx
  fragment are **untouched** — this Decision changes only rendered markup and the
  token values.

**Relationship to D39.** D39 created wiki's landing page (the `internal/web`
package, the handler, the route, the nginx fragment) and owns its behavioral ids
`R-LAND-PG01`, `R-LAND-NMVR`, `R-LAND-CARB`, `R-LAND-ROOT`, `R-LAND-UNGT`. Those
behaviors are **preserved** — the page still renders the service name and version,
returns `200 text/html`, answers the exact root only, and is ungated in-process.
This Decision changes only the *styling and content* of that page, not its
behavior, so it mints **no new ids**; where a D39 test pins the prior markup's
literal text, that assertion is updated to cron's text while the behavioral
assertion stands.

**Rejected.**
- A shared cross-app landing handler — each app still owns its page; this is
  content/style convergence, not code sharing.
- Editing only the eyebrow/description on the existing divergent markup — leaves
  the structural and token differences that are the actual style violation.
- Embedding a frozen copy of cron's HTML in this Decision — would fork the
  canonical page into a drifting copy; pointing at the one cron file keeps a
  single source of truth.

**Verification.** Structural — no `R-XXXX-XXXX` ids (markup/asset content).
Realized by a plan phase whose done bar is a content check:

- `diff wiki/internal/web/static/tokens.css cron/internal/web/static/tokens.css`
  (run from the repo root) prints nothing.
- `wiki/internal/web/landing.tmpl` differs from `cron/internal/web/landing.html`
  on **only** the three lines in the table above — carrying `wiki`,
  `Knowledge base`, and the wiki lead paragraph respectively; all other lines are
  identical.
- The suite stays green: `cd wiki && go build ./...`, `go vet ./...`,
  `gofmt -l .` (prints nothing), `go test ./...`, and `bin/check-migrations wiki`.
