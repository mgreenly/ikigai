# Build artifacts (ignore the built binaries, but track bin/secrets etc.)
/bin/wiki
/bin/eval-extract

# SQLite databases (runtime)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Local environment
.env
.env.local

# Ephemeral build-loop brief (gather writes, verify deletes)
/project/loops/brief.md

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/
