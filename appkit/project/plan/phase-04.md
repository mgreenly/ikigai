# Phase 04 — Retire the stable sibling path (in-repo)

Realizes **D4**, in-repo part only. Fully deterministic and loop-reachable: source
cleanup plus one Go guard test. D4's live-box proof is **not** part of this phase —
it is an operator step in `plan.md` (§ Operator steps), outside the unattended
⬜/✅ set.

## Build

- Confirm no source reader or tool still references the sibling `etc/manifest.env`
  path: grep for `etc/manifest.env` not followed by `current` across
  `appkit/inventory`, `bin/registry`, `bin/start`. Only `etc/current/manifest.env`
  reads should remain. Remove any stragglers.
- In `opsctl/internal/opsctl/layout.go`, fix the stale comment on `ManifestPath`
  ("regenerated by `<app> manifest` on every swap") — nothing maintains that path
  anymore. Remove the `ManifestPath` accessor if it has no remaining caller (it is
  only referenced by `convert.go`, which moves the legacy sibling into the version
  dir; leave `convert`'s one-time migration behavior intact). Do **not** otherwise
  change `opsctl` deploy/setup — prod already publishes `etc/current/manifest.env`.

## Test (`appkit/inventory/inventory_test.go`)

- **R-YSVR-SL00** — a service directory that has **only** `etc/manifest.env` and no
  `etc/current/` is **not** listed by `inventory.Read` (no silent fallback). This is
  the retirement guard; it may sit alongside the Phase 01 tests.

## Done

Appkit suite green (`go build ./...`, `go vet ./...`, `gofmt -l .` empty,
`go test ./...`), sibling references gone, retirement guard passing. Commit.

> The box cleanup (deploy the D1 reader, remove the `/opt/crm/etc/manifest.env`
> bridge + stale siblings, restart the dashboard, verify crm via `/services`) is an
> explicit operator step in `plan.md`, run only on explicit instruction against the
> live `int` account. It does not gate this phase's marker.
