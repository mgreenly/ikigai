# scripts — Plan Status

This is the **manifest**: one line per phase in build order, and the **only**
place a phase's status marker lives. Each phase line begins with the literal word
`Phase` and carries `✅` (done) or `⬜` (not started). The build loop finds its
next unit of work with `grep -nE '^Phase .* ⬜' project/plan/STATUS.md | head -1`,
reads only that phase's `project/plan/phase-NN.md`, builds it, and on completion
flips that one marker. This file deliberately carries **no bare status glyph**
anywhere but on a phase line, so the anchored grep matches only phase lines.

Phase 01 ✅ realizes D1, D2, D3 — landing handler + embedded Carbon template/assets in `internal/web`, wired at `GET /{$}` (ungated in-process) in `cmd/scripts/main.go`
Phase 02 ✅ realizes D4 — nginx fragment: add the exact-match session-gated `= /srv/scripts/` location, validated by a content-assertion test
Phase 03 ✅ realizes D5 — state the landing-page surface in scripts's doctrine (`project/notes/README.md` + `ARCHITECTURE.md`) (structural; docs-only)
Phase 04 ✅ realizes D6 — conform the landing page (markup + tokens) to the cron canonical template; per-service data only (eyebrow + description + title); structural content check
Phase 05 ✅ realizes D7 — add the top-left Home link to the dashboard landing page; covers R-HOME-8R2V
Phase 06 ✅ realizes D8 — self-serve the landing fonts and eliminate the FOUT: relativize the `tokens.css` link, `font-display: swap`→`optional` + self-served `src` in tokens.css, preload display+body fonts with `crossorigin`, and add the session-gated nginx `location /srv/scripts/static/`; covers R-M59W-5CAW, R-M6HS-J41L, R-M8XL-ANIZ, R-MA5H-OF9O, R-MBDE-270D
Phase 07 ✅ realizes D9 — root the rebuildable `runs/` tree under the service-owned `cache/` dir (not the root:root AppDir): collapse `scriptsRuntimeRoot` to `filepath.Dir(cfg.GenerationPath)` in every layout and fix the on-box boot crash-loop (`unlinkat /opt/scripts/runs: permission denied`); update the R-4LKF-FB23 boot/composition tests to the cache location; covers R-RUNS-CDIR, R-RUNS-BOOT
Phase 08 ✅ realizes D10 — adopt the shared `registry` for loopback-port resolution: own port via `registry.MustPort("scripts")`, peer feed defaults + dropbox base via `registry.BaseURL`, add the `registry` require + committed `replace registry => ../registry` to `go.mod`, align the composition-root and nginx-content tests, and add the guardrail test proving no `30xx` literal remains in non-test Go source (behavior-preserving; `etc/manifest.env` byte-identical); covers R-RGST-SELF, R-RGST-PEER, R-RGST-DBOX, R-RGST-NLIT, R-RGST-GMOD
Phase 09 ✅ realizes D11 — consumer loops through `Spec.Consumers` + composition-root normalization: declare the five-upstream `Consumers` table (cron/crm/ledger/dropbox/prompts, each with `consume.Subscriptions([]string{src})` + a handler factory over `svcRef`), delete `runConsumer`/`feedDefault`/`feedURLEnv`/`fromEnv`/`consumerID`/`sources`/the `workers` slice/the `var rt` capture and the legacy `Consumes`+`Subscriptions` fields, collapse `main()` to `appkit.Main(scriptsSpec())` (one fully-formed literal, no post-construction mutation), derive the manifest/boot upstream list from the table (CONSUMES byte-identical), retire R-RGST-PEER; covers R-8WN1-0VQI, R-8XUX-ENH7
Phase 10 ✅ realizes D12 — serve the web surface from `share/www` through the chassis: move `landing.html` + `static/` (tokens.css + four fonts) to `scripts/share/www/`, set `WWW: true`, render via `rt.WWW()`, delete `internal/web` and the service-side `GET /static/` mount (tests relocate to `cmd/scripts` over the shipped tree), boundary-crossing `SCRIPTS_WWW_PATH` export in `bin/start`; covers R-8Z2T-SF7W, R-90AQ-66YL (retained D1/D2/D3/D4/D6/D7/D8 ids re-proven on the new substrate)
Phase 11 ✅ realizes D13 — MCP surface over `appkit/mcp`: `internal/mcp` becomes `Instructions` + `Tools(svc)` (the 16 domain tools) + `NewHandler`, local transport/`Identity`/`toolHealth`/`runtimeContract` deleted, runtime contract moves to `Spec.Health` (feeds HTTP `/health` + MCP `health`), surface becomes 18 tools (16 domain + chassis `health`+`reflection`; `reflection` newly present); covers R-91IM-JYPA, R-92QI-XQFZ
Phase 12 ✅ realizes D14 — delete the `internal/db` `Open`/`Migrate` wrappers (migrations `FS` + load guard remain; harnesses call `appkitdb` directly) and confirm the doctrine states the converted truth (structural; design-doc truth-up authored with the design change set)
Phase 13 ✅ realizes D15 — add `error_page 401 = @login_bounce;` to the two session-gated locations in `scripts/etc/nginx.conf` (`= /srv/scripts/`, `/srv/scripts/static/`), leaving the bearer tier untouched; purely additive, proven by extending the `cmd/scripts/main_test.go` nginx content assertions; covers R-465K-NCPV, R-47DH-14GK, R-49T9-SNXY
Phase 14 ✅ realizes D16 — rename the copy-pasted prompts-named plumbing in `scripts/etc/nginx.conf` (`$prompts_owner`→`$scripts_owner`, `$prompts_client`→`$scripts_client`, `@prompts_authn_500`→`@scripts_authn_500`); behavior identical, proven by extending the `cmd/scripts/main_test.go` nginx content assertions; covers R-4EOV-BQWQ, R-4FWR-PINF
Phase 15 ✅ realizes D17 — event-routing conformance, trigger surface + consumer: triggers become single canonical filter strings (`(script_id, filter)`, literal source segment over the five upstreams, `knownFamilies` + `Registry.CouldMatch`, cron special case deleted), `ScriptsForEvent` matches keys via `routing.Match`, `consume` conforms to `consumer.Event{Kind,Subject}`/`Key()` with `"**"` in-edges, `runs` reshaped to `trigger_kind`+`trigger_subject`, one new `trigger_filters` migration (⛔ eventplane phases 01–04 first); covers R-7TR5-QSY4, R-7UZ2-4KOT, R-7W6Y-ICFI, R-7XEU-W467, R-7YMR-9VWW, R-7ZUN-NNNL, R-812K-1FEA
Phase 16 ✅ realizes D18 — event-routing conformance, producer: completion kinds become `succeeded`/`failed` (the `scripts.` prefix drops), subject `/` + script name, `script.Events` becomes a two-family registry, payload's `trigger` object reshaped to `{source, kind, subject, event_id}`, one new `outbox_routing` migration + a newly added outbox DDL drift guard (⛔ eventplane phases 01–04 first); covers R-82AG-F74Z, R-83IC-SYVO, R-84Q9-6QMD, R-85Y5-KID2
Phase 17 ✅ realizes D19 — structured MCP adoption: the thirteen structured domain tools return `StructuredResult` + declare a verbatim-mirroring `outputSchema` (PascalCase `Script`/`ScriptDetail`/`Trigger`, snake_case `Run`/`FileEntry`), the three prose tools (`describe`, `run_output`, `run_fs_read`) stay `TextResult` schema-less, and `toolResultErr` becomes `structuredError` mapping domain sentinels to closed-vocabulary codes (`not_found`/`validation`/`internal`); no result reshaped, wire surface stays 18 tools; covers R-C0G0-V0QL, R-C1NX-8SHA, R-C2VT-MK7Z, R-C43Q-0BYO, R-C5BM-E3PD, R-C6JI-RVG2, R-C7RF-5N6R, R-CA77-X6O5
Phase 18 ✅ realizes D20 — error-taxonomy enrichment: add `script.ErrTooLarge`/`script.ErrSourceUnavailable` sentinels, reclassify `Import`'s size-cap and mirror-fetch arms, extend `structuredError` with the two matching `errors.Is` arms (`too_large`/`source_unavailable`); UTF-8 and missing-arg rejections stay `validation`, `conflict` stays unused; covers R-CBF4-AYEU, R-CCN0-OQ5J, R-CDUX-2HW8
Phase 19 ✅ realizes D21 — the `suite` module chassis: embedded stdlib-only `internal/runner/suite.py` materialized (0600) beside `main.py` before exec, runtime facts injected as env (`SUITE_SERVICES` from the registry via a new `runner.New` suiteEnv param, `SUITE_FILES_BASE_URL`, per-run `SUITE_SCRIPT_ID`/`SUITE_RUN_ID`/`SUITE_OWNER_EMAIL`), cached verbatim `suite.event()` (`{}` for manual), `ToolError{code,message}`, loud `RuntimeError` outside the runner; establishes the python3 probe-harness; covers R-HVKP-FQRD, R-HWSL-TII2, R-HY0I-7A8R, R-HZ8E-L1ZG
Phase 20 ✅ realizes D22 — `suite.mcp(service, tool, args)`: stateless JSON-RPC `tools/call` with `X-Owner-Email` + `X-Client-Id scripts:<script_id>` (never `X-Forwarded-Proto`), structuredContent returned verbatim, prose fallback to concatenated text, `isError`→`ToolError(code,message)`, unknown service a local `ValueError` (zero HTTP), refused→`source_unavailable`; covers R-I0GA-YTQ5, R-I1O7-CLGU, R-I2W3-QD7J, R-I5BW-HWOX, R-I6JS-VOFM
Phase 21 ✅ realizes D23 and D24 — `suite.fetch` (loopback URL confinement with zero-request rejections, no-redirect streamed GET, temp+rename with same-pass SHA-256, `{path,size,content_hash}`, pinned 404/409/refused/3xx mapping) and `suite.files` (all seven filesystem-API verbs over `SUITE_FILES_BASE_URL`, service-agnostic, `X-Client-Id scripts:<script_id>` only, streaming both directions, verbatim returns with `None` on 204, status-derived failure mapping); covers R-I7RP-9G6B, R-I8ZL-N7X0, R-IA7I-0ZNP, R-IBFE-EREE, R-ICNA-SJ53, R-IDV7-6AVS, R-IF33-K2MH, R-IGAZ-XUD6, R-IHIW-BM3V
Phase 22 ✅ realizes D25 — content-plane holder: `Service.RunContentHandler` (`internal/script/content.go`, `resolveWithin` confinement, `http.ServeContent`, bare-404 error mapping) mounted via `rt.HandleLoopback("GET /run-content", …)`; `FileEntry.ContentURL` populated by the MCP layer for non-dir `run_fs_list` entries from `contentBase = registry.BaseURL("scripts")` threaded through revised `Tools`/`NewHandler` signatures; output schema grows `content_url`; wire surface stays 18 tools; covers R-IIQS-PDUK, R-IJYP-35L9, R-IMEH-UP2N, R-INME-8GTC
Phase 23 ⬜ realizes D26 — `describe` teaches the runtime contract: `describeText` grows the `suite` module (all five surfaces), the `ToolError` closed-vocabulary error model, and products-travel-by-reference guidance, file share framed service-agnostically; stays prose; covers R-IOUA-M8K1
