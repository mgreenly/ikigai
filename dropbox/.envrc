# Inherit the repo-root .envrc (GOFLAGS=-buildvcs=false for the worktree layout).
source_up

export DROPBOX_APP_KEY="$(cat ~/.secrets/DROPBOX_APP_KEY)"
export DROPBOX_APP_SECRET="$(cat ~/.secrets/DROPBOX_APP_SECRET)"
export DROPBOX_REFRESH_TOKEN="$(cat ~/.secrets/DROPBOX_REFRESH_TOKEN)"
