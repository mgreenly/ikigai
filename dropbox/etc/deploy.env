# Workstation-side routing for bin/* scripts. Non-secret.
# dropbox is a path-routed service: it owns no cert, so there is no CERTBOT_EMAIL.
ACCOUNT=int
SSH_USER=ec2-user
SSH_KEY=~/.ssh/id_ed25519_int_ikigenba_com
APEX_SUFFIX=ikigenba.com
