# Build artifacts
/build/

# Local-dev runtime state (dev DB, private mirror, logs — default ./tmp/)
/tmp/

# SQLite databases (runtime; default path is ./tmp/dropbox.db)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Build-loop ephemeral seam (gather writes, verify deletes — never committed)
/project/loops/brief.md

# Local environment
.env
.env.local

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/
