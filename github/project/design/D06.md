# Decision 6 — The landing page and nginx fragment

## Decision

`github` serves the suite's uniform v1 landing page and ships its path-routed
nginx fragment, mirroring `gmail`'s landing (`internal/web`) but with **no**
consent flow and **no** OAuth routes (the GitHub App is pre-installed; there is no
per-user consent).

**`internal/web`** is a copy of the established pattern: an embedded
`landing.html` template rendered with the service name and running version, plus
an embedded `static/` tree (the Carbon `tokens.css` and the woff2 fonts) served
from `/static/`. Wiring in `Handlers` (D1):

```go
rt.Handle("GET /{$}", web.LandingHandler(rt.Service(), rt.Version()))
rt.Handle("GET /static/", http.StripPrefix("/static/", web.StaticHandler()))
```

`landing.html` is the **canonical suite landing layout** — the same
`internal/web/landing.html` structure the other services ship (the current
`gmail` page is the reference). It is non-interactive and shows no GitHub data,
and it renders these fixed elements on the suite design system:

- a `<title>` of `<service> · github`;
- a `Home` link (`<a class="home" href="/">Home</a>`) as the first element inside
  `<main>`;
- the eyebrow **`GitHub connector`**;
- the one-line description **`Github connects the suite to GitHub through one
  shared GitHub App and exposes repository, pull request, and issue actions as MCP
  tools.`** — note it deliberately omits the producers' "…publishes typed events
  to the event plane" clause, because github owns no event-plane producer;
- a Service / Version / API details grid whose **API** cell is
  `POST /mcp`.

The dynamic `service` and `version` values are injected through `html/template`,
so they are HTML-escaped and cannot inject markup. `web.go`, `embed.go`, the
embedded font/CSS assets, and the wiring above are already canonical and stay
put.

**Layout fidelity is pinned, not just content.** The canonical-layout ids below
(`R-7NJI-UTHM`, `R-7PZB-MCZ0`) assert the page's *content* — its title, eyebrow,
description, details grid, and Home link — but a page can carry all of that text
and still render nothing like the suite, because its `<style>` block or markup
has drifted from the canonical structure (a nav link left in normal flow instead
of anchored top-left; a body font naming an **undefined** custom property such as
`var(--font-sans)`, which silently falls back to the browser default serif;
hardcoded pixel spacing instead of the `var(--space-*)` tokens; renamed detail
classes the canonical `<style>` never targets). Content assertions do not catch
that class of drift, so the fidelity of the whole rendered page is pinned two
ways that a wrong-but-content-complete page fails:

- a **token-integrity** check — every `var(--…)` the page references must be a
  token actually defined in the embedded `static/tokens.css`, so no element can
  silently fall back to an unstyled default; and
- a **golden render** — the full HTML `LandingHandler` produces for fixed inputs
  is compared byte-for-byte against a committed golden fixture under
  `internal/web/testdata/`, which encodes the canonical suite layout (markup
  *and* `<style>`), so any structural or stylistic drift from it fails.

The golden fixture is authored from the canonical suite landing (the current
`gmail` page is the reference for structure and `<style>`), carrying github's own
copy; it lives entirely within `github/`'s own tree, so nothing here tests
against a sibling service.

**`etc/nginx.conf`** is the path-routed location fragment (not a vhost), mirroring
`gmail/etc/nginx.conf` with literal `MOUNT=/srv/github/` and port `3203`:

- `= /srv/github/.well-known/oauth-protected-resource` → open PRM bootstrap (no
  auth_request).
- `= /srv/github/` → bare mount root, gated by `auth_request /_session-authn`
  (dashboard browser session), proxied to `/` — the landing page.
- `/srv/github/static/` → session-gated static assets.
- `/srv/github/` (prefix) → every other path gated by `auth_request /_authn` (the
  bearer gate) with the identity-header hygiene and the 429-faithful `error_page`
  block, proxied to loopback `:3203` — this covers `/mcp`.
- `= /srv/github/pr` → `return 404`: the loopback-private twin (D5) must never be
  reachable through the public front door (defence in depth; the handler
  self-guard is primary).

github has **no `/feed`**, so — unlike the producer services — the fragment
carries no `/srv/github/feed` block; nothing under the mount serves a feed.

**`etc/manifest.env`** declares `APP=github`, `MOUNT=/srv/github/`, `PORT=3203`,
`MCP=true`, `DEFAULT=false`, and **no** `FEED=` line and no outbox retention keys
(non-producer, D1). `etc/deploy.env` mirrors the other connectors' workstation
routing.

## Rejected

- **A consent binary (`cmd/consent`) and OAuth callback routes.** gmail needs them
  for per-user Google OAuth; the GitHub App is installed once at the org, so there
  is no consent step and no callback — omitted entirely.
- **A `/feed` block in the fragment.** github publishes no events and mounts no
  feed; a block would guard a route that does not exist.
- **A shared landing handler.** The suite convention is each app owns its landing
  page, template, and embedded assets under its own tree; github embeds its own
  copy of `tokens.css` and the fonts.

## Verification

- `R-EVZ3-VXJZ` — `GET /` returns `200 text/html` containing the service name
  `github` and the running version (from `rt.Service()` / `rt.Version()`).
  Asserted offline against `LandingHandler`.
- `R-7NJI-UTHM` — `GET /` renders the canonical suite landing layout: the
  document `<title>` is `<service> · github`, the page carries the eyebrow
  `GitHub connector` and the exact one-line description above, and a
  Service / Version / API details grid whose **API** cell is `POST /mcp`. A
  degenerate page that shows only name + version fails this. Asserted offline
  against `LandingHandler`.
- `R-7ORF-8L8B` — `LandingHandler` HTML-escapes the injected service and version:
  a service name containing `<script>` and a version containing `&` render as
  escaped entities (`&lt;script&gt;`, `&amp;`) and never as raw markup. Asserted
  offline against `LandingHandler` with hostile inputs.
- `R-7PZB-MCZ0` — the `Home` link (`<a class="home" href="/">Home</a>`) is the
  first element inside `<main>`, so the canonical home affordance leads the page.
  Asserted offline against `LandingHandler`.
- `R-WYSR-NPL3` — every CSS custom property the page references as `var(--NAME)`
  is defined as a `--NAME:` rule in the embedded `static/tokens.css` — in
  particular the `body` font resolves to a defined token (`--font-body`) and the
  page references **no undefined** token such as `var(--font-sans)`, which would
  silently fall back to the browser default serif and break the suite typography.
  A page that names any token `tokens.css` does not define fails. Asserted offline
  by extracting the referenced `var(--…)` name set from the rendered HTML and the
  defined `--…:` name set from `static/tokens.css` and asserting the former is a
  subset of the latter.
- `R-X00O-1HBS` — the full HTML that `LandingHandler` renders for fixed
  `service`/`version` inputs is **byte-identical** to the committed golden fixture
  `internal/web/testdata/landing.golden.html`, which pins the canonical suite
  layout: the `Home` link anchored top-left in `var(--font-mono)`, the body set in
  `var(--font-body)`, `var(--space-*)` token spacing, and the `dl`/`dt`/`dd`/`code`
  Service·Version·API grid. Any drift of the shipped template's markup or `<style>`
  from that pinned layout — a moved or restyled Home link, a hardcoded pixel value,
  a renamed detail class, a different font token — fails. Asserted offline against
  `LandingHandler` with fixed inputs.
- `R-EX70-9PAO` — `GET /static/tokens.css` returns the embedded stylesheet with
  `Content-Type: text/css`, a `.woff2` asset returns `font/woff2`, and a
  traversal path (`../`) or unknown asset returns `404`. Asserted offline against
  `StaticHandler`.
- `R-EYEW-NH1D` — The `etc/nginx.conf` fragment session-gates the bare mount root
  (`auth_request /_session-authn` on `= /srv/github/`), bearer-gates the mount
  prefix (`auth_request /_authn` on `/srv/github/`), returns `404` for
  `= /srv/github/pr`, opens the PRM well-known, and contains **no**
  `/srv/github/feed` block. Asserted by a fragment parse/grep test
  (`internal/web/nginx_test.go`, like gmail's) over the shipped file.
