# Ralph: Select Next Requirement

Pick one pending requirement to work on.

## Pending Requirements

<% pending.each do |req| %>
- `<%= req[:id] %>`: <%= req[:requirement] %>
<% end %>
<% if pending.empty? %>
(none)
<% end %>

## Recent History

```
<%= history %>
```

## Required Output

Output ONLY a JSON object (no markdown fences, no other text):

{"id": "11"}

Use `{"id": "none"}` if no pending requirements.
