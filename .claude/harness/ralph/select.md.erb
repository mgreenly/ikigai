# Select Next Requirement

Pick ONE requirement from the list below. Return its ID as JSON.

## Pending Requirements

<% pending.each do |req| %>
- ID `<%= req[:id] %>`: <%= req[:requirement] %>
<% end %>
<% if pending.empty? %>
(none - all done)
<% end %>

## Recent History

<%= history.empty? ? "(no history yet)" : history %>

## Instructions

1. Look at the Pending Requirements list above
2. Consider dependencies - if a requirement says it depends on others, pick those first
3. Return the ID of your selection as JSON: {"id": "1"}
4. If the list is empty, return: {"id": "none"}
