---
description: Fix the most critical gap in release tasks.
---

Apply the suggested fix using a sub-agent (run_in_background=false). Update `cdd/verified.md` with the resolution. Commit all changes. Suggest the next gap.
