---
description: Fix the most critical gap in release tasks.
---

**Requires:** `$CDD_DIR` environment variable must be set to the workspace directory.

Apply the suggested fix using a sub-agent (run_in_background=false). Update `$CDD_DIR/verified.md` with the resolution. Commit all changes. Suggest the next gap.
